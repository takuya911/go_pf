version: '3.4'
services:
  user:
    build:
      context: ./services/user
      dockerfile: build/Dockerfile.dev
    volumes:
      - ./services/user:/app
    ports:
      - "50051:50051"
    tty: true
