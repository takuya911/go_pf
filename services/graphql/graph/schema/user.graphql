scalar Time
extend type Query {
  getUserById(input:getUserForm!): User
}
extend type Mutation {
  login(input: LoginInput!): LoginPayload!
}

input getUserForm{
  id:ID!
}

extend type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  createdAt: Time!
  updatedAt: Time!
  deletedAt: Time!
}

type TokenPair {
  idToken: String!
}

input LoginInput {
  email: String!
  password: String!
}

type LoginPayload {
  user: User!
  tokenPair: TokenPair!
}
