scalar Time
extend type Query {
  getUser(input:user_id): User!
}
# extend type Mutation {
#   createUser(input: CreateUser!): User!
# }

extend type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  createdAt: Time!
  updatedAt: Time!
  deletedAt: Time!
}

# input CreateUser {
#   id: ID!
#   name: String!
#   email: String!
#   password: String!
# }
