scalar Time
extend type Query {
  getUserById(input:getUserForm!): User
}
# extend type Mutation {
#   createUser(input: CreateUser!): User!
# }

input getUserForm{
  id:ID!
}

extend type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  createdAt: Time!
  updatedAt: Time!
  deletedAt: Time!
}

# input CreateUser {
#   id: ID!
#   name: String!
#   email: String!
#   password: String!
# }
